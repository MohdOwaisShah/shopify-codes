query RunInput {
  cart {
    # request the single attribute by key (must hardcode the key)
    attribute(key: "dataOfShipping") {
      key
      value
    }

    deliveryGroups {
      deliveryOptions {
        title
        handle
      }
    }
  }
}
